{
  "name": "food-free-empty",
  "version": "0.1.0",
  "bin": {
    "food-free-empty": "bin/food-free-empty.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "deploy-prod": "export ENV_NAME=prod && cdk synth && cdk deploy --all --outputs-file outputs.json",
    "deploy-dev": "export ENV_NAME=dev && cdk synth && cdk deploy --all --outputs-file outputs.json",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.142",
    "@types/jest": "^29.5.12",
    "@types/node": "20.14.9",
    "@types/source-map-support": "^0.5.10",
    "aws-cdk": "2.150.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.5.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.150.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}
